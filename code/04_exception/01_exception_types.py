# Unhandled exceptions are error in code.
# We should handle these.
# BaseException is the base class, we should never catch this exception.
# Exception is the sub class of BaseException which we should catch.
